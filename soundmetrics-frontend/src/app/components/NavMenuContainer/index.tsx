import { nav_menu_container } from './styles';

const NavMenuContainer = () => {
  return (
    <div css={nav_menu_container}>
      <h1>SOUNDMETRICS</h1>
    </div>
  );
};

export default NavMenuContainer;
